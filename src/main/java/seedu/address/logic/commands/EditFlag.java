package seedu.address.logic.commands;

/**
 * The flags used by <code>EditCommand</code>. -r corresponds to RESET,
 * -a corresponds to APPEND, no flags corresponds to NONE.
 */
public enum EditFlag {
    APPEND,
    RESET,
    NONE
}
